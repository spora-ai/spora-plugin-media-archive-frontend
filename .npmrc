; Use the public npm registry. Spora does not currently operate a private mirror.
; Bump the save-prefix to caret so version updates inside the declared ranges
; pick up compatible patches automatically (matches Composer semver ^-style).
registry=https://registry.npmjs.org/
save-prefix=^
fund=false
audit=false
engine-strict=true